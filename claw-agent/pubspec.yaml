name: claw_agent
description: Claw Super Agent mobile control center
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  flutter_tts: ^4.0.2
  speech_to_text: ^6.6.2

flutter:
  uses-material-design: true
