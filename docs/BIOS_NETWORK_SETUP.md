# BIOS / Network Setup

To support remote PC wake from phone:
- Enable Wake-on-LAN
- Enable Power on after power loss
- Keep NIC wake enabled
- Configure relay/port forwarding if waking across networks
- Optional: configure a smart plug fallback
